Run a security-focused code review on $ARGUMENTS using the **code-security-auditor** agent.

**$ARGUMENTS:** File path, directory, or "staged" for staged changes (e.g., `src/services/auth.ts`, `src/api/`, `staged`)

Focus on authentication, authorization, input validation, and data exposure risks.
