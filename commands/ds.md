Find opportunities to replace custom UI with design system components, tokens, and patterns from @plt/ui-vue.

**$ARGUMENTS:** Optional file path or directory to scope the audit (defaults to current directory)

Apply **vue-conventions** and **css-conventions** skills.

Focus on buttons, styling, spacing, and common UI patterns. Reference the ui-vue CLAUDE.md for available components.
