Clean up lint issues in $ARGUMENTS: fix type errors, missing imports, unused variables, formatting violations, naming conventions, and resolve all ESLint/TSLint warnings to get a clean lint pass.

**$ARGUMENTS:** File path, glob pattern, or directory (e.g., `src/components/*.vue`, `src/services/`)

Apply **typescript-conventions** skill for type fixes.
